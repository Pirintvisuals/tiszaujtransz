---
deployment:
  tasks:
    - export DEPLOYPATH=$HOME/public_html
    - /usr/bin/rsync -av --no-perms --no-owner --no-group --chmod=F644,D755 --exclude='.git' --exclude='.cpanel.yml' --exclude='*.py' --exclude='*.zip' . $DEPLOYPATH/
